/*

  The Runaway Switch Puzzle

  Expectation:
  When areValuesNonZero is called with either (or both) arguments 0 it
  should return false -- otherwise return true

  Constraints:
  Solve the puzzle using a repeated keyword carelessly left out

*/

const user = {
  name: 'John Doe',
  settings: {
    theme: 'dark',

  }
}

function getConfigValue(key) {
  let result = ''

  switch (expression) {
    case expression:

    default:

  }

  return result
}
