/*

  The Ternary Mind Bender Puzzle

  Expectation:
  When areValuesNonZero is called with either (or both) arguments 0 it
  should return false -- otherwise return true

  Constraints:
  Without adding any lines, add a default to prevent a bad return value

*/

const user = {
  name: 'John Doe',
  settings: {
    theme: 'dark',

  }
}

function getConfigValue(key) {
  const
}
